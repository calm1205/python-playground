services:
  python:
    image: python:3.9
    container_name: python3.9
    build: .
    working_dir: /app
    environment:
      - PYTHONDONTWRITEBYTECODE=1 # __pycache__が生成されなくなる
    volumes:
      - .:/app
    tty: true
